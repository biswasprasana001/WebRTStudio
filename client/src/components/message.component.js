import React from "react";
import '../css/message.component.css';

const Message = () => {
  return (
    <p>
      Expect a delay up to 50 seconds for the backend to load after inactivity,
      as the server restarts with new requests.
    </p>
  );
};

export default Message;
